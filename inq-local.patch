diff --git a/external_libs/gpurun/include/gpu/reduce.hpp b/external_libs/gpurun/include/gpu/reduce.hpp
index 1d429da5..0c33096d 100644
--- a/external_libs/gpurun/include/gpu/reduce.hpp
+++ b/external_libs/gpurun/include/gpu/reduce.hpp
@@ -18,6 +18,7 @@
 #else
 #include <thrust/execution_policy.h>
 #include <thrust/transform_reduce.h>
+#include <cuda/functional>
 #endif
 
 #include <gpu/run.hpp>
@@ -57,7 +58,7 @@ Type run(reduce const & red, Type const init, KernelType kernel) {
 #ifndef ENABLE_GPU
 	return std::transform_reduce(range.begin(), range.end(), init, std::plus<>{}, kernel);
 #else
-	return thrust::transform_reduce(thrust::device, range.begin(), range.end(), kernel, init, std::plus<>{});
+	return thrust::transform_reduce(thrust::device, range.begin(), range.end(), cuda::proclaim_return_type<Type>(kernel), init, std::plus<Type>{});
 #endif
 }
 
@@ -83,13 +84,13 @@ Type run(gpu::reduce const & redx, gpu::reduce const & redy, Type const init, Ke
 
 	auto range = boost::multi::extension_t{0l, sizex*sizey};
 	return thrust::transform_reduce(thrust::device, range.begin(), range.end(),
-																	[sizex, kernel] GPU_LAMBDA (auto ind) {
+																	[sizex, kernel] GPU_LAMBDA (auto ind) -> Type {
 
 																		int ix, iy;
 																		linear_to_bidimensional(ind, sizex, ix, iy);
 																		return kernel(ix, iy);
 																		
-																	}, init, std::plus<>{});
+																	}, init, std::plus<Type>{});
   
 #endif
 }
@@ -119,13 +120,13 @@ Type run(reduce const & redx, reduce const & redy, reduce const & redz, Type con
 
 	auto range = boost::multi::extension_t{0l, sizex*sizey*sizez};
 	return thrust::transform_reduce(thrust::device, range.begin(), range.end(),
-																	[sizex, sizey, kernel] GPU_LAMBDA (auto ind) {
+																	[sizex, sizey, kernel] GPU_LAMBDA (auto ind) -> Type {
 
 																		int ix, iy, iz;
 																		linear_to_tridimensional(ind, sizex, sizey, ix, iy, iz);
 																		return kernel(ix, iy, iz);
 																		
-																	}, init, std::plus<>{});
+																	}, init, std::plus<Type>{});
     
 #endif
 }
diff --git a/src/real_time/viewables.hpp b/src/real_time/viewables.hpp
index 99ef2f07..345b99b1 100644
--- a/src/real_time/viewables.hpp
+++ b/src/real_time/viewables.hpp
@@ -134,7 +134,15 @@ public:
 	auto const & electrons() const {
 		return electrons_;
 	}
-	
+
+	// Project-local extension (jellium KS-energy observables, 2026-05-01).
+	// Exposes the Hamiltonian held by the propagator so external observables
+	// (e.g. inqkit::observables::StateEnergyWriter) can compute
+	// <phi_i|H|phi_i> without rebuilding H from outside. Read-only.
+	auto const & ham() const {
+		return ham_;
+	}
+
 };
 
 }

"""ml-patterns campaign-local kernels (POD, DMD, form factor, normaliser, celldb).

PRE-GATED in T1 (code-test + formula-validation). Not promoted to inqview.
"""
from . import pod, dmd, formfactor, normaliser, celldb  # noqa: F401

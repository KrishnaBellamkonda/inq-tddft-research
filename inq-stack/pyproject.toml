[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "inqview"
version = "0.1.0"
description = "INQ post-processing tools"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "python"}

[tool.setuptools.packages.find]
where = ["python"]
